// set the number of points starting at 0
numPoints = 0;

// function to update the score
function updateScore(numPoints) {
  numPoints++;
  document.getElementById("scoreId").innerHTML = numPoints;
}
